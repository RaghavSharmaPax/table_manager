/**
 * names for input and select tag
 */
enum TagName {
  Row = "rowsInput",
  Col = "colsInput",
  TableName = "tableInput",
  TableSelect = "tableSelect",
}

/**
 * @type NotificationType
 */
enum NotificationType {
  Error = "error",
  Valid = "valid",
  Default = "default",
}
export { NotificationType, TagName };
